# Implementation notes

The source homepage is retained in `docs/original-index.html` for traceability. Its navigation, hero copy, trust strip, services preview, Why TechRizers, selected work, testimonial, WhatsApp CTA and final CTA were used as the homepage reference. The original source references the TechRizers logo asset and SEO organization metadata.

This implementation recreates the visual language in reusable React components rather than copying the old static page as-is, because React routing and API-backed flows require a component application.
