import {Link} from 'react-router-dom';import {useAuth} from '../context/AuthContext';export default function Dashboard(){const {user,logout}=useAuth();return <section className="section"><div className="container"><div className="dashhead"><div><span className="kicker">ACCOUNT</span><h1>Welcome, {user?.name||'User'}</h1></div><button className="btn outline" onClick={logout}>Logout</button></div><div className="grid three"><div className="card"><h3>Profile</h3><p>{user?.email}</p><p>Role: {user?.role}</p></div><div className="card"><h3>Project inquiries</h3><p>View your submitted requests when the backend is connected.</p></div><div className="card"><h3>Payment history</h3><p>Payment records are stored securely by the backend.</p><Link to="/payment">Pay Now</Link></div></div></div></section>}
