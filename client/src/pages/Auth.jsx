import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';import {getAssetUrl} from '../utils/assetUrl';export default function Login(){const [f,setF]=useState({email:'admin@techrizers.com',password:''});const [e,setE]=useState('');const {login}=useAuth();const nav=useNavigate();const submit=async x=>{x.preventDefault();try{const u=await login(f.email,f.password);nav(u.role==='user'?'/dashboard':'/admin/dashboard')}catch(err){setE(err.response?.data?.message||'Login failed')}};return <section className="section"><div className="container narrow"><form className="card form" onSubmit={submit}><div style={{display:'flex',justifyContent:'center',marginBottom:'8px'}}><img src={getAssetUrl('assets/techrizers-mark-square.png')} alt="TechRizers" style={{width:'52px',height:'52px',borderRadius:'12px',boxShadow:'0 4px 16px rgba(37,99,235,0.25)'}} /></div><span className="kicker" style={{textAlign:'center'}}>ACCOUNT LOGIN</span><h1 style={{textAlign:'center'}}>Sign in</h1><label>Email<input required type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>Password<input required type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label>{e&&<div className="alert error">{e}</div>}<button className="btn primary">Login</button><p className="note">Demo admin credentials are documented in README. Change them before production.</p></form></div></section>}
