import {Router} from 'express';import {contact,project,listProjects} from '../controllers/forms.js';import {validate} from '../middleware/validate.js';import {contactSchema} from '../validators/forms.js';import {auth,roles} from '../middleware/auth.js';const r=Router();r.post('/contact',validate(contactSchema),contact);r.post('/project-inquiries',validate(contactSchema),project);r.get('/project-inquiries',auth,roles('admin','editor'),listProjects);export default r;
